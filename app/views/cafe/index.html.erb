<h1>Welcome to Coder Cafe</h1>

<table>
    <tr>
        <td>Item</td>
        <td>Price</td>
    </tr>
    <% @menu.each do |key, val| %>
    <tr>
        <td><%= key.capitalize %></td>
        <td><%= "$%.2f" % val %></td>
    </tr>
    <% end %>
</table>